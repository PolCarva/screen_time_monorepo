import { OnboardingFlow } from "@/components/onboarding/onboarding-flow";

/** The story, then the setup (docs/onboarding-v2-plan.md). */
export default function OnboardingScreen() {
  return <OnboardingFlow />;
}
