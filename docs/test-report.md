# Verification report

Run date: 2026-08-31 through 2026-09-01.

## Automated checks

| Check                               | Result                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm check`                        | Passed: ESLint, TypeScript, and 53 unit tests (contracts 11, mobile 15, web 27).                                                                                                                                                                                                                                                                                                   |
| Expo Doctor                         | Passed all 20 applicable checks after aligning Expo SDK 57 patch releases and React Native 0.86.3. Native configuration is covered by committed Android/iOS regression assertions.                                                                                                                                                                                                 |
| Production dependency audit         | `pnpm audit --prod --audit-level=moderate` exits successfully. Its sole registry advisory, CVE-2026-45822, is explicitly suppressed only after backporting the official `decode-uri-component` 0.5.0 linear decoder into Expo Router's CommonJS-compatible 0.2.2 dependency; regression coverage processes 30,000 malformed characters below the 250 ms bound.                                                         |
| Clean Supabase migration + seed     | Passed on the local PostgreSQL 17 Supabase image through migration `202608310002`.                                                                                                                                                                                                                                                                                                 |
| `supabase test db`                  | Passed 16 pgTAP database invariants covering RPC grants, operational switches, bounded active reward intents, reconciliation/debt behavior, and deletion recovery.                                                                                                                                                                                                                 |
| `supabase db lint`                  | No errors; only intentional unused wire-compatibility parameters were reported at warning level.                                                                                                                                                                                                                                                                                   |
| `pnpm --filter web build`           | Passed production compilation and route generation.                                                                                                                                                                                                                                                                                                                                |
| Android `:app:compileDebugKotlin`   | Passed with min SDK 29, compile/target SDK 36. Only platform/dependency deprecation warnings remain.                                                                                                                                                                                                                                                                               |
| Dormant iOS spike compile           | Previously passed for Still and its four extension targets with signing disabled. It is retained as future source evidence and is not part of the Android v1 release gate.                                                                                                                                                                                                         |
| Production web smoke                | Playwright verified `/`, `/impact`, and `/privacy` with no browser console, page, or real network errors. After deployment `b52521a`, `/`, `/impact`, `/privacy`, `/terms`, `/api/v1/config`, and `/api/v1/impact/current` all returned HTTP 200; the homepage served its Google ownership tag.                                                                                          |
| Production config fail-closed check | `APP_VARIANT=production` correctly rejects a build with a missing EAS project ID or without Google identity explicitly enabled.                                                                                                                                                                                                                                                    |
| `git diff --check`                  | Passed.                                                                                                                                                                                                                                                                                                                                                                            |
| Production Supabase                 | All migrations through `202608310002` applied to `unyhkgmdqbtrmkoxlqnk`; Google Auth is enabled, Apple Auth is disabled, and public product metrics remain real rather than seeded.                                                                                                                                                                                                |
| Production data/API                 | Real charities and Android-only policy v3 published: 10-minute unlocks, 3 daily emergency passes, 10 rewarded ads/day, balance cap 3, AdMob active, Android restrictions active, and iOS restrictions off. The open week reports real zero values without demo fallbacks.                                                                                                          |
| Authenticated production E2E        | Passed registration, wallet, wellbeing sync, AdMob intent creation, Emergency Unlock, complete privacy export, account deletion, token invalidation, six-table cascade verification, and financial-ledger pseudonymization with a disposable anonymous account.                                                                                                                    |
| Production Vercel cron              | Authenticated reward reconciliation returned HTTP 200 with `{ "reconciled": 0 }`.                                                                                                                                                                                                                                                                                                  |
| Production AdMob configuration      | Approved account; real Android app and rewarded-unit IDs; SSV points to the production webhook; European and US-state UMP messages are published.                                                                                                                                                                                                                                  |
| Production AdMob Reporting          | Google OAuth refresh, AdMob account lookup, and network report generation passed. Vercel holds the credentials as production secrets; the authenticated job returned HTTP 200 and persisted 14 rows for `2026-08-18` through `2026-08-31`, all `admob_api`/`estimated` with real zero pre-traffic totals.                                                                            |
| Production Android AAB              | EAS build [`9ac820c1-bd89-4552-9bb3-ea590e25f6c1`](https://expo.dev/accounts/goshops/projects/still-screen-time/builds/9ac820c1-bd89-4552-9bb3-ea590e25f6c1) finished from commit `dbc22ed` with store signing, package `com.still.screentime`, version code 6, and a downloadable [AAB artifact](https://expo.dev/artifacts/eas/EMdM13KULhgtZfjX0Wx3tYVVFp1E4maF33Yz0lObn6c.aab). |

Unit coverage targets contract/domain transitions, offline wallet projection, Google-only voting identity, analytics consent, stable mobile API errors, the patched URI decoder's malformed-input complexity, AdMob SSV signature transport, reward-intent signing, HTTP error secrecy/mapping, OAuth redirect safety, security headers, transactional waitlist rate limiting, donation-proof signature validation, asynchronous persisted beta signup behavior, and committed native identity/entitlement configuration.

## Remaining external state

- Google identity is configured in Supabase and EAS, Google Auth Platform is published for external users, Search Console ownership is verified, and AdMob Reporting is connected. Google's brand-review appeal is submitted; its external decision and a signed-device identity link/return test remain.
- Android Accessibility timing and OEM background behavior require real devices and Play review.
- A real Android AdMob test-device impression, SSV callback, UMP presentation, and provider-link flow require a signed physical-device run. The Reporting API import is verified; the Android AdMob app remains pending Play-listing association.
- iOS and Apple Developer enrollment are outside the current release scope and do not gate Android v1. The iOS Shortcuts pause has local evidence only (next section); every physical-device observation is still pending.

## iOS Shortcuts flow — 2026-09-20

Local evidence for the unreleased iOS pause (`ios-shortcuts.md`). The rows above are the Android v1 record and were not re-run as a whole.

| Check | Result |
| --- | --- |
| `pnpm check` | Passed: lint, TypeScript and 179 unit tests (contracts 15, web 29, mobile 135). |
| `pnpm build` | Passed, including the Next.js production build. |
| `supabase test db` | Passed 25 pgTAP invariants on the local stack after `supabase migration up --local` applied `202609050001` and `202609200001`. Not applied to production. |
| `supabase db lint --local` | No errors; the same intentional unused-parameter warnings as before. |
| iOS simulator build | `xcodebuild` Debug / iphonesimulator with signing disabled succeeded on Xcode 26.0.1 for Still and its four extensions, with no warnings in `StillNative`. |
| `acceptance:ios-shortcuts` | Passed against that build: the action is discoverable, does not open Still unconditionally, keeps dynamic foregrounding, offers the chosen apps as options, and carries the production iOS AdMob app id. |
| iOS 26.0 simulator walkthrough | Passed for everything after the trigger, driven by shortcuts that run Still's action (recipe and observations in `ios-shortcuts.md`). It found and fixed two bugs: the return to the app always failed because `Linking.openURL` was passed unbound, and one failed open disabled an app's URL scheme for good. "App is opened" automations cannot fire in the simulator, so the trigger itself is unproven. |
| Physical iPhone | **Not run.** The twelve-point checklist in `ios-shortcuts.md` and hypotheses H1–H9 are pending. Nothing about automation timing, foregrounding without a dialog, URL-scheme return, the Home Screen exit or ad delivery on iOS has been observed. |

Use the acceptance matrix in `native-feasibility.md` before promoting beyond a closed beta.
